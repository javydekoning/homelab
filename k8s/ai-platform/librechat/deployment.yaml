apiVersion: apps/v1
kind: Deployment
metadata:
  name: librechat
  labels:
    app.kubernetes.io/name: librechat
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: librechat

  template:
    metadata:
      labels:
        app.kubernetes.io/name: librechat
    spec:
      serviceAccountName: librechat
      securityContext:
        fsGroup: 2000
      containers:
        - name: librechat
          securityContext:
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            runAsUser: 1000
          image: ghcr.io/danny-avila/librechat:latest@sha256:7a806208c9e88e7de8a91e9e56d5295e3e8c280ce25bb77470bbac4eb5e4a455
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: 3080
          readinessProbe:
            httpGet:
              path: /health
              port: 3080
          envFrom:
            - configMapRef:
                name: librechat-envs
            - secretRef:
                name: librechat-credentials-env
          volumeMounts:
            - name: librechat-config-volume
              mountPath: /app/librechat.yaml
              subPath: librechat.yaml
              readOnly: true
            - name: librechat-images
              mountPath: /app/client/public/images
      volumes:
        - name: librechat-config-volume
          configMap:
            name: librechat-config
        - name: librechat-images
          hostPath:
            path: /config/librechat/images
            type: DirectoryOrCreate
