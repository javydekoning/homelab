apiVersion: v1
kind: Service
metadata:
  name: mcpo
  namespace: openwebui
  labels:
    app: mcpo
spec:
  selector:
    app: mcpo
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  type: ClusterIP
