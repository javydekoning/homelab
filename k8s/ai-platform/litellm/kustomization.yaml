apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: litellm
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - ns.yaml
  - external-secret.yaml
configMapGenerator:
  - name: litellm-config
    files:
      - config.yaml=./litellm_config.yaml
