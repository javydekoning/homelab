apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: zigbee2mqtt
    spec:
      containers:
        - name: app
          securityContext:
            privileged: true
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: persistence
              mountPath: /app/data
              subPath: zigbee2mqtt
            - name: usb
              mountPath: /dev/ttyUSB0
            - name: udev
              mountPath: /run/udev
        - name: zigbee2mqtt-exporter
          image: ghcr.io/d0ugal/zigbee2mqtt-exporter:latest@sha256:5ba56076aecabf02440fea1d519002c76d7f683831c968370e7bc3c06ff88278
          ports:
            - name: metrics
              containerPort: 8087
          env:
            - name: Z2M_EXPORTER_WEBSOCKET_URL
              value: "ws://localhost:8080/api"
            - name: Z2M_EXPORTER_LOG_LEVEL
              value: "info"
      volumes:
        - name: usb
          hostPath:
            path: /dev/ttyUSB0
        - name: udev
          hostPath:
            path: /run/udev
