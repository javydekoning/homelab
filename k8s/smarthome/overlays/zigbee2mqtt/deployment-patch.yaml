apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  template:
    spec:
      containers:
        - name: app
          securityContext:
            privileged: true
          ports:
            - name: http
              containerPort: 8080
          volumeMounts:
            - name: persistance
              mountPath: /app/data
              subPath: zigbee2mqtt
            - name: usb
              mountPath: /dev/ttyUSB0
            - name: udev
              mountPath: /run/udev
        - name: zigbee2mqtt-exporter
          image: ghcr.io/d0ugal/zigbee2mqtt-exporter:latest@sha256:7a668b5616e66b4235377015fe11e37699556c56967ecbbe38f7e443707ef6e2
          ports:
            - name: metrics
              containerPort: 8087
          env:
            - name: Z2M_EXPORTER_WEBSOCKET_URL
              value: "ws://localhost:8080/api"
            - name: Z2M_EXPORTER_LOG_LEVEL
              value: "info"
      volumes:
        - name: usb
          hostPath:
            path: /dev/ttyUSB0
        - name: udev
          hostPath:
            path: /run/udev
