apiVersion: v1
kind: Service
metadata:
  name: nutritrace
  namespace: nutritrace
  labels:
    app: nutritrace
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3001
      targetPort: 3001
  selector:
    app: nutritrace
