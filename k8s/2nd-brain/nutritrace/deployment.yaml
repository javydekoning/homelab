apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nutritrace
  name: nutritrace
  namespace: nutritrace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nutritrace
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: nutritrace
    spec:
      containers:
        - name: nutritrace
          image: ghcr.io/traceapps/nutritrace:dev@sha256:cfdd8c424e2e232bbcb10712e3472d42e767f31d018c86f0384cf8b7aee36c15
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: nutritrace-config
            - secretRef:
                name: nutritrace-credentials
          ports:
            - containerPort: 3001
              name: http
              protocol: TCP
          volumeMounts:
            - mountPath: /data/db
              name: db
            - mountPath: /data/uploads
              name: uploads
      restartPolicy: Always
      volumes:
        - name: db
          hostPath:
            path: /config/nutritrace/db
            type: DirectoryOrCreate
        - name: uploads
          hostPath:
            path: /config/nutritrace/uploads
            type: DirectoryOrCreate
