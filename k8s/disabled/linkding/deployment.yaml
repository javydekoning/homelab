apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: linkding
  name: linkding
  namespace: linkding
spec:
  selector:
    matchLabels:
      app: linkding
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: linkding
    spec:
      containers:
        - name: linkding
          envFrom:
            - configMapRef:
                name: linkding-config
            - secretRef:
                name: linkding-credentials
          image: ghcr.io/sissbruecker/linkding:latest@sha256:61b2eb9eed8e5772a473fb7f1f8923e046cb8cbbeb50e88150afd5ff287d4060
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9090
              name: http
              protocol: TCP
          volumeMounts:
            - mountPath: /etc/linkding/data
              name: appdata
      restartPolicy: Always
      volumes:
        - name: appdata
          hostPath:
            path: /config/linkding
            type: DirectoryOrCreate
