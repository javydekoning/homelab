apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openwebui

resources:
  - deployment.yaml
  - external-secret.yaml
  - service.yaml

configMapGenerator:
  - name: mcpo-config
    files:
      - config.json=./config.json
