apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: slskd

resources:
  - ns.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - envs:
      - ./resources/slskd.env
    name: slskd-envs
