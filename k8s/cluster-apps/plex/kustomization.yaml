apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dvr

helmCharts:
  - name: plex
    repo: https://k8s-at-home.com/charts/
    version: 6.4.3
    releaseName: plex
    namespace: dvr
    valuesFile: values.yaml
    includeCRDs: true

resources:
  - ns.yaml
