apiVersion: v1
kind: Namespace
metadata:
  name: mount-s3
  labels:
    # Loosen Pod Security so the CSI controller can run.
    # You can tighten this later if the upstream chart becomes fully restricted-compliant.
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted

