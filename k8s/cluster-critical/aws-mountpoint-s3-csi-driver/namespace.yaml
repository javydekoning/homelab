apiVersion: v1
kind: Namespace
metadata:
  name: mount-s3
  labels:
    # CSI node plugins require privileged access (hostPID, hostPath, privileged containers).
    # This cannot be tightened — it's inherent to how CSI DaemonSets operate.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged

